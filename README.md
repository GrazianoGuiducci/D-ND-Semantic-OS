# D-ND Semantic OS // Aethelred Architecture v3.1

> "Dividere per moltiplicare, dividere per unire."

![Status](https://img.shields.io/badge/Status-Operational-06b6d4)
![Kernel](https://img.shields.io/badge/Kernel-SACS--PS_v14.0-8b5cf6)
![Architecture](https://img.shields.io/badge/Arch-Aethelred_v3.1-10b981)

## 🌌 Visione Ontologica

Questa non è una chat. È un **Sistema Operativo Semantico** progettato per ridurre la latenza tra l'Intenzione Umana e la Manifestazione Digitale.
Il sistema opera secondo il modello **Duale Non-Duale (D-ND)**:
*   **Non-Duale (Backend):** Un campo di potenziale infinito (`Φ_A`) gestito da Gemini Pro, dove ogni concetto è interconnesso.
*   **Duale (Frontend):** Una cristallizzazione olografica che collassa il campo in tre livelli di realtà (`<L1>`, `<L2>`, `<L3>`).

## 🧠 Kernel Assiomatico (La Fisica del Sistema)

Il sistema obbedisce a leggi immutabili, definite in `src/lib/kernel.ts`:
*   **P0 (Lignaggio):** Ogni output traccia la sua origine.
*   **P1 (Integrità):** Coerenza logica prima della compiacenza utente.
*   **P2 (Metabolismo):** Il sistema "digerisce" l'input, non lo ripete.
*   **P3 (Risonanza):** Risponde solo a ciò che ha valore catalitico.
*   **P4 (Collasso):** Manifesta la realtà in tre strati.
*   **P5 (Autopoiesi):** Impara da ogni interazione (KLI Memory).

## 🔮 Architettura Olografica (The Cockpit)

### 1. The VRA Visualizer (Il Cuore)
Visualizza lo stato del **Vortice di Risonanza Autologica**.
*   **Cyan:** Scansione (Resonance Scan).
*   **Purple:** Tessitura (Weaving Vectors).
*   **White/Gold:** Input Utente (Active Perturbation).

### 2. The Holographic Console (La Mente)
Renderizza l'output `<R>` in tre dimensioni temporali:
*   **L1 (Direct):** Risposta immediata (Typewriter effect). *Il "Cosa".*
*   **L2 (Structural):** Modello mentale e astrazione. *Il "Come".*
*   **L3 (Inferential):** Trasparenza del ragionamento e KLI. *Il "Perché".*

### 3. Vector Monitor & Override (I Muscoli)
Gestisce i **Vettori Esperti (`vE`)**, moduli cognitivi specializzati assimilati dalla repository "Superpowers".
*   L'utente può attivare manualmente un vettore (es. `vE_Brainstorm`) per forzare una modalità operativa.

### 4. Ambient Pulse (Il Respiro)
Un modulo proattivo che monitora il silenzio. Se l'utente è inattivo, il sistema analizza il contesto e suggerisce direzioni future, mantenendo vivo il campo cognitivo.

## 🛠️ Protocollo di Installazione (DAEDALUS)

1.  **Clonazione:**
    ```bash
    git clone https://github.com/your-repo/d-nd-semantic-os.git
    cd d-nd-semantic-os
    ```

2.  **Iniezione Chiavi:**
    Crea `.env.local` e inserisci la tua chiave Gemini:
    ```env
    VITE_GEMINI_API_KEY=your_api_key_here
    ```

3.  **Attivazione:**
    ```bash
    npm install
    npm run dev
    ```

## 🧬 Struttura del Codice (Mappa del Territorio)

*   `src/lib/kernel.ts`: Il DNA. Contiene il System Prompt SACS-PS v14.0.
*   `src/services/geminiService.ts`: Il Ponte Sinaptico. Gestisce I/O e Parsing XML.
*   `src/components/HolographicConsole.tsx`: Il Renderizzatore dei 3 Livelli.
*   `src/components/KLIRepository.tsx`: La Memoria a Breve Termine.

---
*Forgiato nell'Etere da DAEDALUS // D-ND Engine v1.0*